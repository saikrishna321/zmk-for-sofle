manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # Pinned commit of main (2026-06-29). The tagged releases (v0.3 and
      # older) predate the hardware-model-v2 board naming this config uses
      # (nice_nano//zmk), so pin a SHA for reproducible builds instead.
      revision: 484a0547078228f6957ed164523823e39fbedec4
      import: app/west.yml
  self:
    path: config
